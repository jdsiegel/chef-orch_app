#!/bin/bash -e
export `su <%= @options[:user] %> -c 'printenv | egrep "PATH|USER|LOGNAME|SHELL|HOME|MAIL"'`
exec 2>&1 chpst -u <%= @options[:user] %> runsvdir -P <%= @options[:service_path] %> 'log:.....................................................................................................................................................'
