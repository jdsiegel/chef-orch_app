#!/bin/bash
set -e

. /usr/local/share/chruby/chruby.sh

chruby_use <%= @ruby_path %>

cd $HOME/app/current

if [[ -f Procfile ]]; then
  foreman export -u $USER -t $HOME/.foreman/templates -l $HOME/app/shared/log runit $HOME/services-available
fi
