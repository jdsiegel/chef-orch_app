#!/bin/bash
. /etc/profile.d/chruby.sh
. /etc/profile.d/app_env.sh
cd /home/<%= user %>/app/current
exec chpst -e <%= File.join(location, "#{process_directory}/env") %> sh -c 'exec <%= process.command %>'
